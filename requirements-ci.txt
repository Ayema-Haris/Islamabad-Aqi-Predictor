# requirements-ci.txt
# Python 3.11 compatible (your CI currently uses 3.11, which is fine)
# KEY FIX: hsfs and hsml are bundled INSIDE hopsworks[python] since v4.x
# Do NOT pin hsfs or hsml separately — that was causing your errors

# ── Hopsworks (includes hsfs + hsml internally) ──────────────────
hopsworks[python]==4.7.2

# ── Core ML ──────────────────────────────────────────────────────
pandas==2.1.4
numpy==1.26.4
scikit-learn==1.4.2
joblib==1.3.2

# ── Models ───────────────────────────────────────────────────────
xgboost==2.0.3

# ── Data serialisation (required by hopsworks internally) ────────
pyarrow==15.0.0